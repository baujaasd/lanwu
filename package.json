{
  "name": "lanwu",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pug src/ -o dist/ -P",
    "watch": "pug src/ -o dist/ -P -w",
    "serve": "browser-sync start --server 'dist' --files 'dist/*.html, src/css/*.css'",
    "deploy": "gh-pages -d dist",
    "dev": "npm-run-all --parallel watch serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.5",
    "gh-pages": "^6.1.1",
    "npm-run-all": "^4.1.5"
  }
}
